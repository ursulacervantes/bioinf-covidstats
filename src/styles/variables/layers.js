// Elements absolutely overlaying the page, such as dialog windows, tooltips,
// dropdowns…
export const Z_OVERLAY = 100

// Elements above the page level such as headers, footers, floating or fixed
// containers…
export const Z_ABOVE = 50

// Elements needing to be explicitly bumped atop of their siblings within their
// stacking context.
export const Z_BUMP = 1

// Elements needing to be explicitly moved below their siblings within their
// stacking context.
export const Z_BELOW = -1
