
import { StyleSheet } from 'fela-tools'

const title = {
    fontSize: '24px',
    fontWeight: 500,
    marginBottom: '20px',
    letterSpacing: '.3px',
    lineHeight: '1.4',
}

const list = {
    margin: 0,
    padding: 0,
    listStyle: 'none',
}

const listItem = {
    marginBottom: '20px',
}

export default StyleSheet.create({
    title,
    list,
    listItem,
})


