query(
  $dateFrom: String
  $dateTo: String
  $sex: String
) {
  results: getComorbidity(
    dateFrom: $dateFrom
    dateTo: $dateTo
    sex: $sex
  ) {
     data {
      name
      value
      error
     }
     maxValue
  }
}